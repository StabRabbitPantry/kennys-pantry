import { createSlice } from "@reduxjs/toolkit"


const initialState = {
    // fillll in with proper stuff
}

export const ingredientsSlice = createSlice({
    name:

});

export const {
    // list reducers here
} = ingredientsSlice.actions;

export default ingredientsSlice.reducer;